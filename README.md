# FastAPI REST API с Чистой Архитектурой

Этот проект представляет собой веб-приложение на FastAPI, построенное с использованием принципов Чистой Архитектуры. Он служит практическим примером для организации кода в сложных и масштабируемых приложениях.

На данный момент реализован RESTful API для управления "Постами" с полным набором CRUD-операций.

## 🚀 Технологии

- **Python 3.11+**
- **FastAPI** - для создания асинхронного REST API.
- **SQLAlchemy 2.0** - для асинхронного взаимодействия с базой данных (ORM).
- **Pydantic** - для валидации данных и управления настройками.
- **Poetry** - для управления зависимостями и окружением.
- **Uvicorn** - в качестве ASGI-сервера.

## 🏗️ Структура проекта

Проект строго следует принципам **Clean Architecture**, разделяя код на независимые слои:

- `cmd/`: Точка входа в приложение (`main.py`).
- `src/core/`: Ядро бизнес-логики. Содержит доменные модели и абстракции (например, абстрактные репозитории). Не зависит ни от одного другого слоя.
- `src/application/`: Слой приложения. Содержит сценарии использования (Use Cases) и объекты передачи данных (DTO). Координирует ядро и внешние слои.
- `src/infrastructure/`: Реализация внешних сервисов. Здесь находится конкретная реализация репозиториев для работы с базой данных, конфигурация подключения к БД и т.д.
- `src/interfaces/`: Адаптеры к внешнему миру. В данном случае — это API-роутеры FastAPI, которые обрабатывают HTTP-запросы.
- `tests/`: Директория для тестов.

## ⚙️ Установка и запуск

1.  **Клонируйте репозиторий:**
    ```bash
    git clone <your-repo-url>
    cd <project-folder>
    ```

2.  **Настройте переменные окружения:**
    Создайте файл `.env` в корне проекта. Вы можете скопировать `.env.example`, если он есть, или создать файл с нуля. Укажите в нем URL для подключения к вашей базе данных:
    ```env
    DB_URL=postgresql+asyncpg://user:password@host:port/dbname
    ```

3.  **Установите зависимости:**
    Убедитесь, что у вас установлен [Poetry](https://python-poetry.org/docs/#installation). Затем выполните команду:
    ```bash
    poetry install
    ```

4.  **Запустите приложение:**
    Для запуска сервера в режиме разработки с автоматической перезагрузкой используйте команду:
    ```bash
    poetry run uvicorn cmd.main:app --reload
    ```

5.  **Откройте документацию API:**
    Сервер будет доступен по адресу `http://127.0.0.1:8000`.
    Интерактивная документация Swagger UI находится по адресу: **[http://127.0.0.1:8000/docs](http://127.0.0.1:8000/docs)**.

## Endpoints API

Реализованы следующие эндпоинты для управления постами:

- `POST /posts/`: Создание нового поста.
- `GET /posts/`: Получение списка постов (поддерживает пагинацию через query-параметры `start` и `end`).
- `GET /posts/{post_id}`: Получение одного поста по его ID.
- `PUT /posts/{post_id}`: Обновление поста по ID.
- `DELETE /posts/{post_id}`: Удаление поста по ID.